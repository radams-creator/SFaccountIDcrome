{
  "manifest_version": 3,
  "name": "Salesforce Record ID Copier",
  "version": "1.0.0",
  "description": "Copy the Salesforce Record ID from Lightning record URLs via the right-click context menu.",
  "permissions": ["contextMenus", "scripting", "clipboardWrite"],
  "host_permissions": [
    "https://*.lightning.force.com/*"
  ],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
